## Linux 设置

### BBR 加速配置

查看内核版本（需要 4.9 以上）：

```bash
uname -a
```

开启 BBR：

```bash
echo -e "net.core.default_qdisc=fq\nnet.ipv4.tcp_congestion_control=bbr" >> /etc/sysctl.conf
```

关闭 IPv6：

```tsx
echo -e "net.ipv6.conf.all.disable_ipv6=1\nnet.ipv6.conf.default.disable_ipv6=1\nnet.ipv6.conf.lo.disable_ipv6=1" >> /etc/sysctl.conf
```

应用配置：

```tsx
sysctl -p
```

### 防火墙端口配置

放行所有端口：

```bash
sudo iptables -P INPUT ACCEPT
sudo iptables -P FORWARD ACCEPT
sudo iptables -P OUTPUT ACCEPT
sudo iptables -F
sudo iptables-save
sudo apt install -y iptables-persistent  # 安装
sudo netfilter-persistent save  # 保存并设置为开机启动
sudo netfilter-persistent reload  # 重新载入
sudo service netfilter-persistent restart
```

放行特定端口（以 8384 为例）：

```bash
sudo iptables -I INPUT -p udp --dport 8384 -j ACCEPT
sudo iptables -I INPUT -p tcp --dport 8384 -j ACCEPT
```

### Nginx 反向代理配置

```bash
server {
    listen 443 ssl;
    server_name [linkroad.io](http://linkroad.io);
    ssl_certificate /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/private.key;
    
    location / {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_redirect off;
        proxy_pass http://127.0.0.1:8080;
        
        # 如果您要使用本地存储策略，请将下一行注释符删除，并更改大小为理论最大文件尺寸
        client_max_body_size 20000m;
    }
}
```

---

## Windows 系统错误修复

### 开始菜单修复

**问题**：关键错误，你的开始菜单出现了问题。我们将尝试在你下一次登录时修复它。

**解决步骤**：

1. 进入"此电脑"，依次打开 `C:\Windows\System32\WindowsPowerShell\v1.0`
2. 找到 `powershell.exe`，右键选择"以管理员身份运行"
3. 在 PowerShell 窗口中复制并执行以下命令：

```bash
Get-AppXPackage -AllUsers | Foreach {Add-AppxPackage -DisableDevelopmentMode -Register "$($_.InstallLocation)\AppXManifest.xml"}
```

1. 等待命令运行完毕后重启系统
2. 重新登录后开始菜单即可恢复正常

### 内网路由设置

添加静态路由：

```json
route add 192.9.0.0 mask 255.255.0.0 192.168.165.200 -p
```

---

## Windows 无网络安装

### 跳过联网要求

**首次开机设置页面操作**：

1. 按 `Shift + F10` 打开命令行（部分笔记本需按 `Fn + Shift + F10`）
2. 输入以下命令并回车：

```bash
oobe\bypassnro
```

> 💡 如果无法输入文字，需要先用鼠标点击窗口激活
> 
1. 电脑自动重启后，联网界面会多出"我没有 Internet 连接"选项，点击即可继续设置

**替代方法**：

在命令提示符中依次执行：

```jsx
reg add HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\OOBE /v BypassNRO /t REG_DWORD /d 1 /f
shutdown /r /t 0
```

或使用本地账户设置：

```jsx
start ms-cxh:localonly
```

### 系统激活

```json
irm https://get.activated.win | iex
```

---

## Dropbox 命令行工具

### 常用命令列表

```jsx
dropbox autostart    # 设置开机自动启动
dropbox exclude      # 排除目录不同步
dropbox filestatus   # 查看文件同步状态
dropbox help         # 显示帮助信息
dropbox lansync      # 启用或禁用局域网同步
dropbox ls           # 列出目录内容及同步状态
dropbox proxy        # 设置代理
dropbox running      # 检查 Dropbox 是否运行
dropbox sharelink    # 获取文件共享链接
dropbox start        # 启动 Dropbox
dropbox status       # 查看当前同步状态
dropbox stop         # 停止 Dropbox
dropbox throttle     # 设置带宽限制
dropbox update       # 下载最新版本
dropbox version      # 显示版本信息
```

---

## Windows 目录文件名提取

### 方法 1：使用命令提示符（CMD）

1. 按 `Win + R`，输入 `cmd`，回车打开命令提示符
2. 执行以下命令：

```
dir "C:\Windows" /ad /b > folders.txt
```

**参数说明**：

- `/ad` - 仅显示文件夹
- `/b` - 简洁格式输出
- `> folders.txt` - 将结果保存到文件

### 方法 2：使用 PowerShell

1. 按 `Win + S`，搜索并打开 PowerShell
2. 执行以下命令：

```
Get-ChildItem -Path "C:\Windows" -Directory | Select-Object Name | Out-File "folders.txt"
```

> ⚠️ **注意事项**：
> 

> - 如果 Windows 安装在其他驱动器，请修改路径（如 `D:\Windows`）
> 

> - 某些系统文件夹需要管理员权限，可右键选择"以管理员身份运行"
> 

---

## 安装 Docker / Docker Compose

### 更新软件源

```jsx
apt-get update && apt-get -y upgrade
```

### 安装 Docker

```jsx
curl -fsSL https://get.docker.com | sh
```

### 安装 Docker Compose

**卸载旧版本**：

- 如果之前安装了 2.0 以下版本：

```jsx
sudo rm /usr/local/bin/docker-compose
```

- 如果之前安装了 2.0 以上版本：

```jsx
rm -rf .docker/cli-plugins/
```

**安装最新版本**：

```jsx
sudo apt-get update && sudo apt-get install docker-compose-plugin -y
```

**验证安装**：

```jsx
docker compose version
```

**更新 Compose**：

```jsx
sudo apt-get update && sudo apt-get upgrade -y
```
